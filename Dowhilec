#include<stdio.h>
#include<conio.h>
int main()
{
	int i;
	int a[10];
	do
	{
		printf("enter the roll no %d",i);
		scanf("%d",&a[i]);
		i++;
	}while(i<10);
	printf("\n the roll nos were ");
	for(i=0;i<10;i++)
	{
		printf("\n %d", a[i]);
	}
}
